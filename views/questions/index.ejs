<% layout("layouts/boilerplate.ejs") %>

<h1>All Questions</h1>    
<ul>
    <% for(let question of questions){ %>
        <li><a href="/questions/<%= question._id %>" ><%= question.question %> </a> - <b><%= question.author %></b></li>
    <% } %>
</ul>

